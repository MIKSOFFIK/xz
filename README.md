# FNaE

## запуск
**для запуска нужно открыть `start.bat`**

## горячие клавиши:

- esc - выхот в главное меню
- spase(пробел) - открытие камер

```
sudo apt install python3-dev python3-pip libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev libfreetype-dev libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev
```